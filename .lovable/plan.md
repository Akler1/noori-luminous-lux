

# Update UNICEF Supporting Text

## Change

Update the paragraph in `src/components/StickyStoryRefined.tsx` (line 51) from:

> "A portion of every Noori purchase supports UNICEF programs for child safety and education."

To:

> "A portion of every Noori purchase supports UNICEF programs for children to get out of dangerous mines and into the classroom."

## Technical Detail

Single text change on line 51 of `src/components/StickyStoryRefined.tsx`. No other files affected.

